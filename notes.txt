so, i was thinking maybe do an if statement to check if it goes below a specific number? That way i wouldn't have to introduce c++
threading to the application, however I probably will have to once i implement gui functionality. 


~issues 

the bloody build memory address function needs to be analysed and looked at because there's something in there that's stuffing up read process and ruining the final memory addresses. 
